# Application Design

## Generalization Over Hardcoding

This app processes arbitrary forms and documents in any language. Fixes and features must be domain-agnostic and language-agnostic. Never hardcode logic for a specific form, document type, dataset, or language. Design solutions that work across all instances of the same category.
